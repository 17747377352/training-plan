<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trainingplan.platform.mapper.BrowserUploadCredentialMapper">

    <!-- 清空旧 DI 令牌后，已有的服务端定时采集会自动跳过该账号。 -->
    <update id="activateBrowserAccount">
        UPDATE garmin_account
        SET token_ciphertext = NULL, auth_status = 'ACTIVE', sync_enabled = 1
        WHERE id = #{accountId}
    </update>

    <!-- 一个账号只保留一个上传凭据；重新配对即撤销旧凭据。 -->
    <insert id="upsertCredential">
        INSERT INTO browser_upload_credential (garmin_account_id, token_hash)
        VALUES (#{accountId}, #{tokenHash})
        ON DUPLICATE KEY UPDATE token_hash = VALUES(token_hash), create_time = CURRENT_TIMESTAMP(3)
    </insert>

    <select id="selectAccountIdByTokenHashForUpdate" resultType="java.lang.Long">
        SELECT garmin_account_id
        FROM browser_upload_credential
        WHERE token_hash = #{tokenHash}
        FOR UPDATE
    </select>

    <delete id="deleteByAccountId">
        DELETE FROM browser_upload_credential WHERE garmin_account_id = #{accountId}
    </delete>
</mapper>
