# 项目开发进度

最后更新：2026-09-20

## 当前阶段

阶段二：多用户账号与认证。

## 已完成

- [x] 创建 Git 单仓库目录结构。
- [x] 创建 JDK 17 + Spring Boot 3.5 后端工程。
- [x] 接入 MyBatis-Plus、MySQL、Redis、Flyway、Validation、Security 和 Actuator。
- [x] 建立统一返回值、错误码、业务异常和全局异常处理框架。
- [x] 建立 MyBatis-Plus 分页配置及 Controller、Service、ServiceImpl 分层示例。
- [x] 创建 Vue 3 + TypeScript + Vite 前端工程。
- [x] 接入 Vue Router、Pinia、Axios、Element Plus 和 ECharts。
- [x] 建立统一 Axios 请求封装、路由和系统状态首页。
- [x] 创建 Python 3.12 Collector 工程。
- [x] 接入 `garminconnect[typed]`、Redis、HTTPX、Pydantic Settings 和结构化日志。
- [x] 建立本地 `dev` 配置忽略规则和可提交的示例配置。
- [x] 创建本地 MySQL 数据库 `training_plan`。
- [x] 编写首个 Flyway 数据库迁移脚本。
- [x] Java、Vue、Python 三端构建与基础测试通过。
- [x] 后端启动成功，Flyway 已在本地执行 V1 迁移。
- [x] 后端健康接口返回统一 `Result<T>`，本地数据库表已验证。
- [x] 项目首次提交并推送至 GitHub `main` 分支。
- [x] Java 后端和 Python Collector 均已通过 Redis 5 号库连接验证。
- [x] 完成用户、角色和用户角色的 MyBatis-Plus 数据模型。
- [x] 完成用户注册、用户名/邮箱登录和当前用户查询。
- [x] 接入 Spring Security JWT 无状态认证和 BCrypt 密码摘要。
- [x] 完成 access token + refresh token 机制，refresh token 在 Redis 中进行一次性轮换和退出撤销。
- [x] 完成 Vue 登录、注册、路由保护、登录态恢复与自动刷新令牌。
- [x] 认证服务单元测试通过，并使用本地 MySQL/Redis 完成注册、登录、刷新轮换、退出和 401 端到端验证。
- [x] 完成令牌类型隔离：资源服务器只接受 access token，refresh token 只能用于换发，避免刷新令牌被当作接口凭证。
- [x] 刷新令牌消费改为 Redis 原子操作，并发重复使用同一 refresh token 时只有一次换发成功。
- [x] 新增 `JwtConfigTest` 令牌类型隔离单元测试，并补充可重复执行的端到端断言脚本 `server/scripts/verify-auth-e2e.sh`。

## 进行中

- 暂无。账号认证主链路已完成，准备进入 Garmin 账号绑定。

## 下一步

- [ ] Garmin 账号绑定和 MFA 流程。
- [ ] Collector Redis 任务消费框架。
- [ ] Garmin Token 加密存储。
- [ ] 每日健康、睡眠、HRV 和训练准备度同步。

## 约定

- 所有真实密码和 Token 只保存在被 Git 忽略的本地开发配置中。
- 自定义 SQL 统一放在 `server/src/main/resources/mapper/`。
- 所有接口返回统一的 `Result<T>`。
- 每完成一个可验证功能，同步更新本文件。
