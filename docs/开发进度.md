# 项目开发进度

最后更新：2026-09-20

## 当前阶段

阶段二：多用户账号与认证。

## 已完成

- [x] 创建 Git 单仓库目录结构。
- [x] 创建 JDK 17 + Spring Boot 3.5 后端工程。
- [x] 接入 MyBatis-Plus、MySQL、Redis、Flyway、Validation、Security 和 Actuator。
- [x] 建立统一返回值、错误码、业务异常和全局异常处理框架。
- [x] 建立 MyBatis-Plus 分页配置及 Controller、Service、ServiceImpl 分层示例。
- [x] 创建 Vue 3 + TypeScript + Vite 前端工程。
- [x] 接入 Vue Router、Pinia、Axios、Element Plus 和 ECharts。
- [x] 建立统一 Axios 请求封装、路由和系统状态首页。
- [x] 创建 Python 3.12 Collector 工程。
- [x] 接入 `garminconnect[typed]`、Redis、HTTPX、Pydantic Settings 和结构化日志。
- [x] 建立本地 `dev` 配置忽略规则和可提交的示例配置。
- [x] 创建本地 MySQL 数据库 `training_plan`。
- [x] 编写首个 Flyway 数据库迁移脚本。
- [x] Java、Vue、Python 三端构建与基础测试通过。
- [x] 后端启动成功，Flyway 已在本地执行 V1 迁移。
- [x] 后端健康接口返回统一 `Result<T>`，本地数据库表已验证。
- [x] 项目首次提交并推送至 GitHub `main` 分支。
- [x] Java 后端和 Python Collector 均已通过 Redis 5 号库连接验证。
- [x] 完成用户、角色和用户角色的 MyBatis-Plus 数据模型。
- [x] 完成用户注册、用户名/邮箱登录和当前用户查询。
- [x] 接入 Spring Security JWT 无状态认证和 BCrypt 密码摘要。
- [x] 完成 access token + refresh token 机制，refresh token 在 Redis 中进行一次性轮换和退出撤销。
- [x] 完成 Vue 登录、注册、路由保护、登录态恢复与自动刷新令牌。
- [x] 认证服务单元测试通过，并使用本地 MySQL/Redis 完成注册、登录、刷新轮换、退出和 401 端到端验证。
- [x] 完成令牌类型隔离：资源服务器只接受 access token，refresh token 只能用于换发，避免刷新令牌被当作接口凭证。
- [x] 刷新令牌消费改为 Redis 原子操作，并发重复使用同一 refresh token 时只有一次换发成功。
- [x] 新增 `JwtConfigTest` 令牌类型隔离单元测试，并补充可重复执行的端到端断言脚本 `server/scripts/verify-auth-e2e.sh`。
- [x] V2 迁移为 `garmin_account` 增加 `region` 字段，并将唯一键改为 `user_id + region + garmin_email_hash`（国际站与国区站点是两套账号体系，同邮箱可跨站并存）。
- [x] 补齐接口级授权：新增 `PageParam`、`PageResult<T>` 分页基类，`AdminController` 以 `@PreAuthorize("hasRole('ADMIN')")` 开放用户列表与状态管理。
- [x] 修复鉴权异常被兜底处理器吞掉的问题：`@PreAuthorize` 拒绝原先返回 HTTP 200 与「系统暂时不可用」，现单独处理 `AccessDeniedException` 并返回 403 与 `FORBIDDEN`。
- [x] 修复 Web 层切片测试无法启动的问题：`@MapperScan` 从启动类移到 `MybatisPlusConfig`。
- [x] 新增 `AdminUserServiceTest` 与 `AdminControllerSecurityTest`，覆盖禁用自己、状态幂等、未知用户、匿名 401、普通用户 403 和管理员放行。

### 阶段二：Garmin 账号绑定

- [x] 新增 AES-GCM Token 加密服务，每条记录独立随机 IV，并以账号 ID 作为附加认证数据，密文无法搬到其他账号记录解密。
- [x] 新增 Garmin 账号实体、Mapper 与接口：列表、连接、提交 MFA、校验令牌、自动同步开关、删除。
- [x] Spring 通过内网 HTTP 客户端调用 Collector，Collector 不可达时返回 `GARMIN_COLLECTOR_UNAVAILABLE`。
- [x] Collector 新增 FastAPI 内网接口与 Garmin 登录服务，MFA 会话保存在进程内存（带 TTL 与容量上限），凭据校验失败一律拒绝。
- [x] Collector 把 Garmin 失败原因映射为 `INVALID_CREDENTIALS`、`RATE_LIMITED`、`MFA_INVALID`、`TOKEN_INVALID`、`FAILED`，平台侧对应到具体错误码，不把底层异常透给前端。
- [x] 连接失败时清理本次新建的占位账号记录，避免账号列表出现从未绑定成功的幽灵账号。
- [x] 建立 Garmin 只读方法白名单：以 `get_` 前缀为主，显式封禁 `query_garmin_graphql`（可执行 mutation）、`request_reload`（只读名字实为 POST）、`remove_gear_from_activity`（写操作但不带被禁前缀），并用 AST 测试锁死"官方库不存在发写请求的 get_ 方法"这一前提。
- [x] 实现令牌恢复：`restore_session` 用已存令牌恢复会话，令牌失效时把账号置为 `REAUTH_REQUIRED`，对应验收标准"重新启动服务后可以使用 Token 恢复连接"。
- [x] 新增前端 Garmin 账号页面：连接表单、MFA 验证码弹窗、账号列表、校验令牌、自动同步开关与删除，满足"可在网页端完成 MFA"。
- [x] 端到端验证 Spring → Collector → Garmin 完整链路：假凭据返回 `46007 Garmin账号或密码错误`，库中不残留记录，明文密码与令牌均未入库。
- [x] 新增可重复执行的令牌恢复验证脚本 `server/scripts/verify-garmin-token.sh`，用伪造令牌走通"平台解密 → Collector 恢复会话 → Garmin 拒绝 → 状态置为需要重新认证"，无需真实 Garmin 账号。

## 进行中

- 暂无阻塞项。阶段二代码与可自动化验证均已完成。

## 下一步

- [ ] 用真实 Garmin 账号走一遍连接与 MFA 全流程（需用户本人凭据与验证码）。
- [ ] Collector Redis 任务消费框架。
- [ ] 每日健康、睡眠、HRV 和训练准备度同步。

## 已知设计约束

- **Garmin 登录耗时长，超时必须逐层放大。** 官方库会依次尝试多段登录策略，实测国际站一次失败登录约 30 秒。因此超时层级必须是「前端 150 秒 > 后端调采集器 120 秒 > 采集器等 Garmin」，任何一层小于实际耗时都会把"Garmin 慢"误报成别的故障（曾出现前端 15 秒超时、后端报"采集服务不可用"两种误报）。
- **Garmin 会对登录端点限流。** `sso.garmin.com` 在频繁尝试后会直接返回 429 或 HTML 挑战，库的登录链会逐个策略重试到超时。实测一次被限流的登录会在日志里留下五段失败记录（`mobile+cffi`、`mobile+requests`、`widget+cffi`、`portal+cffi` 均为 429，`portal+requests` 为 403 Cloudflare 人机挑战）。采集器已把这种情况区分为 `RATE_LIMITED` 与 `UNREACHABLE`，并且**一旦出现就进入冷却期**（`rate_limit_cooldown_seconds`，默认 900 秒），冷却期内直接拒绝请求、不再触碰 Garmin，避免重复尝试延长限流。排查时不要反复重试。
- **浏览器能登录不代表程序能登录。** 浏览器能通过 Cloudflare 人机挑战并使用既有会话，而程序登录必须走未认证的 SSO 端点，会被 429/403 拦下。因此"网页能登"不能作为"凭据或网络没问题"的结论。
- **MFA 会话不能放进 Redis。** `python-garminconnect` 的 `resume_login(client_state, mfa_code)` 需要一个存活的客户端对象，`client_state` 不可序列化。会话因此保存在 Collector 进程内存（`MfaSessionStore`，TTL 600 秒、上限 100 个），Redis 只存「会话 ID → 账号 ID」映射用于归属校验。**Collector 首期必须单副本部署**，或对 `loginSessionId` 做粘性路由，否则提交验证码的请求可能落到没有该会话的副本。
- **Token 加密密钥必须备份。** `app.security.token-cipher-key` 丢失后已存储的 Garmin Token 无法解密，所有账号都需要重新认证。
- **禁用用户的即时性。** access token 是无状态的，禁用后最长 15 分钟内仍能通过签名校验；但 `login`、`refresh` 和 `UserService.getProfile` 都会检查状态，因此禁用用户无法登录、无法续期，业务接口也会被拒。新增业务接口时需沿用"读取用户状态"的做法。
- **首个管理员需人工提升。** 注册只分配 `USER` 角色，平台未提供创建管理员的接口，需在数据库中手动授予 `ADMIN`。
- **删除账号暂不级联删除同步数据。** `sync_job` 对 `garmin_account` 有外键约束，目前没有同步数据所以删除可用；接入同步后需要在同一事务里先清理同步数据。

## 约定

- 所有真实密码和 Token 只保存在被 Git 忽略的本地开发配置中。
- 自定义 SQL 统一放在 `server/src/main/resources/mapper/`。
- 所有接口返回统一的 `Result<T>`。
- 每完成一个可验证功能，同步更新本文件。
