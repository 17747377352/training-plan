# 项目协作约定

本文件是给 AI 协作代理的项目级说明，会在会话中自动加载。

## 提交与推送

- **只 commit，不要 push。** 本机到 `github.com` 的连通性不稳定，反复重试会浪费大量时间。
  提交留在本地，由项目所有者手动推送。
- 提交信息使用中文，格式 `<类型>: <描述>`，类型用 `feat` / `fix` / `docs` / `refactor` / `test` / `chore`。
- 存在多个待推提交时不影响继续开发。

## 文档同步

- 每完成一个可验证的功能，同步更新 `docs/开发进度.md`：
  - 「已完成」按阶段追加条目
  - 「下一步」移除已完成项、补充新发现的工作
  - 踩到的坑写进「已知设计约束」，并说明**现象 → 原因 → 规避方式**
- 文档里的数字（代码量、测试数、迁移版本、数据量）应当与实际核对后再写，不要凭印象。

## 凭据与隐私

- 真实密码、Token、密钥只放在被 Git 忽略的本地配置中（`server/src/main/resources/application-dev.yml`、
  `collector/.env.dev`、`web/.env.development`）。
- 本地留存的 Garmin 令牌放在 `storage/`（已在 `.gitignore` 中）。
- **提交前扫描**：确认待提交内容里不含本地配置的值。Garmin 活动数据含 GPS 坐标、位置名与
  账号姓名，按开发计划 §12 要求一律不采集、不存储。

## 数据库

- 结构变更一律通过 `server/src/main/resources/db/migration/` 下的 Flyway 迁移，**不要手工执行 DDL**；
  迁移一旦应用就不可修改，需要变更时新增下一个版本。
- 含数字的字段名要注意 MyBatis-Plus 的列名推导规则（仅在大写字母前插下划线，数字前后不插），
  必要时用 `@TableField` 显式映射；`EntitySchemaConsistencyTest` 会拦住错配。

## 验证要求

- 声称功能"完成"之前，要有可复现的证据：单元测试通过 + 真实链路跑通（涉及数据库或外部接口时）。
- 测试要验证**能捕获到目标缺陷**，而不只是"恰好通过"；例如静态一致性测试应先制造一次错配确认它会失败。
